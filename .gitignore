# Secrets
**/.env
*.env
!.env.example

# Backups (server-side hand-edit history — not tracked)
*.bak
*.bak-*
*.bak.*
*.bak_*
index.html.bak*
entry-*.js.bak*
*-backup-*.js
map-picker-backup.html

# Dependencies / build
node_modules/
# ...ama Expo web export'unun ikon/font asset'leri "assets/node_modules/" altında
# yaşıyor ve dağıtılması ZORUNLU (yoksa ikonlar boş kare çıkar) — istisna:
!frontend/assets/node_modules/
!frontend/assets/node_modules/**
venv/
__pycache__/
*.pyc
.expo/
dist/
build/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Claude Code local (personal) settings — shared .claude/settings.json is fine to commit
.claude/settings.local.json

# Uploaded user content (pulled for reference; large & churny)
backend/uploads/

# Local dev venv (Windows)
.venv/
backend/uploads/


# Large production snapshots (kept on server + here; put on GitHub Releases, not git)
_archive/

